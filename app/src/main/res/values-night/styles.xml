<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" >
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat" />

    <style name="DefaultTheme" parent="android:Theme.DeviceDefault.Light.DarkActionBar">
        <item name="android:colorBackground">@android:color/darker_gray</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:fastScrollStyle">@style/FastScroll</item>
        <item name="android:fastScrollPreviewBackgroundLeft">
            @drawable/fast_scroll_drawable_background
        </item>
        <item name="android:fastScrollPreviewBackgroundRight">
            @drawable/fast_scroll_drawable_background
        </item>
    </style>

    <style name="FastScroll" parent="android:Widget.DeviceDefault.Light.FastScroll">
        <item name="android:textColor">#fafafa</item>
    </style>
</resources>
