<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="DefaultTheme" parent="android:style/Theme.DeviceDefault.Light">
        <item name="android:fastScrollStyle">@style/FastScroll</item>
        <item name="android:fastScrollPreviewBackgroundLeft">@drawable/fast_scroll_drawable_background</item>
        <item name="android:fastScrollPreviewBackgroundRight">@drawable/fast_scroll_drawable_background</item>
    </style>

    <style name="FastScroll" parent="android:Widget.DeviceDefault.Light.FastScroll">
        <item name="android:textColor">#fafafa</item>
    </style>
</resources>
